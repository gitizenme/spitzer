<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="Spitzer.Views.ItemDetailPage"
             Title="{Binding Title}">

    <ScrollView>
        <StackLayout Spacing="20">
            <Label Text="{Binding Item.Title}" d:Text="Title" FontSize="Small"/>
            <Image Source="{Binding Item.ImagePreview}"
                    HorizontalOptions="CenterAndExpand"
                    VerticalOptions="Center"
                    Aspect="AspectFill"/>
            <Label Text="{Binding Item.Description}" d:Text="Description" FontSize="Small"/>
            <StackLayout x:Name="Images">
            </StackLayout>
            <StackLayout x:Name="Metadata">
            </StackLayout>
        </StackLayout>
    </ScrollView>
    
</ContentPage>